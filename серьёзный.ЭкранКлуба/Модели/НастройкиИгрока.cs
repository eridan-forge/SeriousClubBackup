using System;
using System.Collections.Generic;

namespace серьёзный.ЭкранКлуба.Модели
{
    public class НастройкиИгрока
    {
        public Guid АккаунтId { get; set; }

        public List<Guid> Избранное { get; set; } = new();

        public List<Guid> ПоследниеИгры { get; set; } = new();

        // Игры, которые игрок скрыл у себя из карусели. НЕ влияет на
        // каталог других игроков и на Игра.Скрыта (это поле админа) -
        // это персональный фильтр отображения, хранится в том же
        // JSON-файле, что и Избранное/ПоследниеИгры. Старые файлы без
        // этого поля десериализуются нормально - Скрытые останется
        // пустым списком (значение по умолчанию из инициализатора).
        public List<Guid> Скрытые { get; set; } = new();
    }
}