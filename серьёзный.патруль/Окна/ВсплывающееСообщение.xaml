<Window x:Class="серьёзный.Патруль.Окна.ВсплывающееСообщение"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        Width="380"
        Height="150">

    <Border Background="#1E293B"
            CornerRadius="16"
            BorderBrush="#334155"
            BorderThickness="1"
            Padding="16">

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0">

                <TextBlock x:Name="Имя"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="White"
                           VerticalAlignment="Center"/>

                <Button DockPanel.Dock="Right"
                        Width="28"
                        Height="28"
                        Content="✕"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="White"
                        Click="Закрыть_Click"/>

            </DockPanel>

            <TextBlock x:Name="Текст"
                       Grid.Row="1"
                       Margin="0,10,0,10"
                       Foreground="White"
                       FontSize="14"
                       TextWrapping="Wrap"/>

            <Button Grid.Row="2"
                    Width="100"
                    Height="34"
                    HorizontalAlignment="Right"
                    Content="Ответить"
                    Click="Ответить_Click"/>

        </Grid>

    </Border>

</Window>