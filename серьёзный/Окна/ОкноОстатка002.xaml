<Window x:Class="серьёзный.Окна.ОкноОстатка002"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Остаток времени"
        Width="470"
        Height="500"
        MinHeight="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#0F172A">

    <Grid Margin="24">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock
            x:Name="Имя"
            Grid.Row="0"
            FontSize="28"
            FontWeight="Bold"
            Foreground="White"
            TextWrapping="Wrap"
            Margin="0,0,0,8"/>

        <TextBlock
            Grid.Row="1"
            Foreground="#94A3B8"
            Text="На аккаунте осталось время."
            TextWrapping="Wrap"
            Margin="0,0,0,20"/>

        <Border
            Grid.Row="2"
            Background="#1E293B"
            CornerRadius="12"
            Padding="16"
            Margin="0,0,0,24">

            <StackPanel>

                <TextBlock
                    Text="Осталось на аккаунте"
                    Foreground="#94A3B8"/>

                <TextBlock
                    x:Name="Остаток"
                    FontSize="34"
                    FontWeight="Bold"
                    Foreground="#93C5FD"
                    Margin="0,6,0,0"/>

            </StackPanel>

        </Border>

        <Grid Grid.Row="4">

            <Grid.RowDefinitions>
                <RowDefinition Height="46"/>
                <RowDefinition Height="46"/>
                <RowDefinition Height="46"/>
            </Grid.RowDefinitions>

            <Button
                Grid.Row="0"
                Content="▶ Использовать остаток"
                Click="Использовать"/>

            <Button
                Grid.Row="1"
                Margin="0,10,0,0"
                Content="➕ Новое время, остаток оставить"
                Click="Новое"/>

            <Button
                Grid.Row="2"
                Margin="0,10,0,0"
                Content="✕ Отмена"
                Click="Отмена"/>

        </Grid>

    </Grid>

</Window>