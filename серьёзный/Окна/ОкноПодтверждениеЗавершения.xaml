<Window x:Class="серьёзный.Окна.ОкноПодтверждениеЗавершения"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Завершение сеанса"
        Width="460"
        Height="340"
        MinHeight="340"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#0F172A">

    <Grid Margin="24">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Text="⏹"
            FontSize="42"
            HorizontalAlignment="Center"
            Foreground="#F87171"/>

        <TextBlock
            Grid.Row="1"
            Text="Завершить сеанс?"
            FontSize="24"
            FontWeight="Bold"
            Foreground="White"
            HorizontalAlignment="Center"
            Margin="0,8,0,12"/>

        <TextBlock
            x:Name="Описание"
            Grid.Row="2"
            TextWrapping="Wrap"
            TextAlignment="Center"
            VerticalAlignment="Center"
            Foreground="#94A3B8"
            Margin="10,0,10,16"/>

        <Grid Grid.Row="3">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button
                Grid.Column="0"
                Height="46"
                Margin="0,0,6,0"
                Content="Отмена"
                Click="Отмена_Click"/>

            <Button
                Grid.Column="1"
                Height="46"
                Margin="6,0,0,0"
                Content="⏹ Завершить"
                Foreground="#FCA5A5"
                Click="Завершить_Click"/>

        </Grid>

    </Grid>

</Window>